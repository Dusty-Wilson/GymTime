<% if user_signed_in? %>
  <%= form_for :visit, :url => {:action => "index", :controller => "visits"} do |f| %>
    <%= hidden_field_tag :user_id, current_user.id %>
    <%= f.submit "I went to the Gym" %>
  <% end %>
  <% end %>
